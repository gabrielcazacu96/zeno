{
  "name": "zeno",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "check-types": "turbo check-types",
    "ci": "turbo run build check-types lint test e2e",
    "ci:force": "turbo run build check-types lint test e2e --force",
    "ci:graph": "turbo run build check-types lint test e2e --graph=turbo-graph.html",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix"
  },
  "devDependencies": {
    "turbo": "2.5.5",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@10.6.2",
  "engines": {
    "node": ">=18"
  }
}
