{
  "name": "zeno",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "check-types": "turbo check-types",
    "ci": "pnpm run lint && turbo run build check-types test e2e",
    "ci:force": "turbo run build check-types test e2e --force",
    "ci:graph": "turbo run build check-types test e2e --graph=turbo-graph.html",
    "dev": "turbo dev",
    "format": "ultracite format",
    "lint": "ultracite lint"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.2",
    "turbo": "2.5.5",
    "typescript": "5.8.3",
    "ultracite": "5.1.1"
  },
  "packageManager": "pnpm@10.6.2",
  "engines": {
    "node": ">=18"
  }
}
